import { TiptapEditorDirective } from './tiptap-editor.directive';

describe('TiptapEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new TiptapEditorDirective();
    expect(directive).toBeTruthy();
  });
});
